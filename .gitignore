# --- AnchorForge: Secrets & Local Config ---
# Keep templates but ignore the real deal
!local_config/.env.template
!local_config/dot_env_template
local_config/.env
local_config/.env.*.bak
local_config/.env.py
local_config/Keystore/
local_config/*.json

# Important: Secure specific key files
key_pairs.json

# --- AnchorForge: Runtime Data & Caches ---
# Ignore everything in these folders...
cache/*
database/*
output/*
runtime/*
playground/*

# ...but keep the folders themselves via .keep or .gitkeep
!cache/.keep
!cache/.gitkeep
!database/.keep
!database/.gitkeep
!output/.keep
!output/.gitkeep
!runtime/.keep
!runtime/.gitkeep
!playground/.keep
!playground/.gitkeep

# Specific sub-patterns
cache/wallet/
cache/*.sim.json

# --- Legacy & Backups ---
backup/
BU/
Legacy/
genesis_info/
old_logs/
audit_log_main/

# --- Python & IDEs ---
venv/
.venv/
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.idea/
.vscode/

# --- OS & Temporary Files ---
.DS_Store
Thumbs.db
*.log
*.tmp
*.ps1
Remarks-bsv-sdk.txt
.env
.flaskenv

# --- Project Specific ---
Doc/
tests/
